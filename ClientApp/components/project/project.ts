import Vue from 'vue';
import {Component} from "vue-property-decorator";
const echarts = require('echarts');

interface Project {
    title: string;
    name: string;
    
}

// @Component
// export default class ProjectComponent exte
